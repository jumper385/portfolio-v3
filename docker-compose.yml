version: "3.3"

services:
  db:
    build: ./db
    expose:
      - 8080
    ports:
      - 8543:8080
    volumes:
      - ./db/pb_data:/pb/pb_data
  client:
    build: ./client
    ports:
      - 80:3000
    expose:
      - 80
    depends_on:
      - db
